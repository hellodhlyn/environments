# Set prefix to <C-a>
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# Panes
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Bindings

# Reloads
bind r source-file ~/.tmux.conf
